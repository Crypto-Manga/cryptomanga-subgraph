{
  "name": "cryptomanga-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cryptomanga-subgraph",
    "create-local": "graph create --node http://localhost:8020/ cryptomanga-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ cryptomanga-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cryptomanga-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
